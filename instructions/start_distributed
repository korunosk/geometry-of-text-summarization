### Create Conda environment on a particular location
conda create --prefix /scratch/korunosk/conda-envs/dlab

### Activate Conda environment from a particular location
eval "$(conda shell.bash hook)"
conda activate /scratch/korunosk/conda-envs/dlab

### Installing Conda kernel in Jupyter
python3 -m ipykernel install --user

### Listing and removing kernels
jupyter kernelspec list
jupyter kernelspec uninstall [kernel]
